from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
    version="0.1.0",
    description="Return demographics based on isochrones around a given location.",
    author="cavalier",
    license="",
)
